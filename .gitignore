node_modules/
chats/
settings.json
*.log
.DS_Store
Thumbs.db
# uploadsは追跡するが、中身のファイルは除外
public/uploads/*
!public/uploads/.gitkeep
# HTTPS証明書（秘密鍵を誤コミットしないため）
cert.pem
key.pem
# Google Drive のリフレッシュトークン / サービスアカウントキー（機密）
gdrive_token.json
gdrive-service-account*.json
# 配布用ZIP（更新ファイルをまとめた成果物。ソースではないので追跡しない）
opengeek-llmchat-*.zip
# Python venv
.venv-transcribe/
# Python キャッシュ
__pycache__/
*.pyc
# ML/RL の実行時データ (データセット・学習済みモデル等)
ml/
# マルチLLM: llama-serverが報告した実測VRAM（環境依存のキャッシュ）
vram-measured.json
